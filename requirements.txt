pandas>=2.0
numpy>=1.24
scipy>=1.10
# Pinned to a minor range: the trained models in models/ are committed
# pickles, and unpickling an estimator built by a different scikit-learn
# raises InconsistentVersionWarning and is explicitly not guaranteed to
# produce correct results. Bump this deliberately and retrain, not by drift.
scikit-learn>=1.9,<1.10
pyarrow>=14.0
xgboost>=2.0
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
requests>=2.31
# Prediction-log persistence. Optional at runtime: without DATABASE_URL the
# log falls back to a JSON file, so local development needs no database.
psycopg[binary]>=3.1
